# 更新 CLI

| 能力 | 命令 | 说明 |
|---|---|---|
| 检查并安装更新 | `update` | 有副作用；会尝试更新本机 CLI。 |
| 强制更新 | `update --force` | 高风险；忽略版本比较并尝试替换本机 CLI。 |

命令：

```cmd
yzj-cli update
yzj-cli update --force
```

执行前先确认：

```text
准备执行：
命令：
影响：
是否确认继续？
```
